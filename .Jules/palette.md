## 2024-11-21 - Accessible Form Labels
**Learning:** Form fields without explicit `id`/`for` associations are inaccessible to screen readers and have smaller click targets, making them harder to use for all users.
**Action:** Always link labels to inputs using explicit `id` and `for` attributes to ensure programmatic association.
