## 2025-05-14 - Accessible and Functional File Upload
**Learning:** Converting a static placeholder into a functional file upload area with visual feedback and keyboard accessibility significantly improves the user experience and inclusivity.
**Action:** Use the `<label>`-linked hidden `<input type="file">` pattern for custom upload UI, and always provide immediate feedback (e.g., file names) and keyboard listeners.
