## 2024-12-10 - Form Label Accessibility
**Learning:** In static HTML forms, failing to link labels to inputs via `for` and `id` attributes significantly hinders accessibility for screen reader users and reduces the interactive area for all users.
**Action:** Always verify that every form label has a `for` attribute matching the `id` of its corresponding input, and ensure `name` attributes are present for proper form submission.
